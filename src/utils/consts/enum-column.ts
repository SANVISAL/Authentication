
export enum Gender {
  male = "male",
  female = "female",
  other = "other",
  unknown = "unknown",
}

export enum Roles {
  user = "user",
  admin = "admin",
  superAdmin = "super_admin",
}

export enum SessionStatus {
  active = "active",
  expire = "expired",
  terminated = "terminated",
}
